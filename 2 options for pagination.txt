two practical options are:
✅ Option 1: On-demand single-page fetch (ideal for UI, APIs, Tableau)

DECLARE @PageSize INT = 500;
DECLARE @PageNumber INT = 3;

SELECT *
FROM dbo.fn_GetRiskValuesRaw()
ORDER BY asof_dt DESC
OFFSET (@PageNumber - 1) * @PageSize ROWS
FETCH NEXT @PageSize ROWS ONLY;
Use this when user/UI specifies a page — very fast and scalable.

✅ Option 2: Fetch all pages using a loop + temp table (previous example)
Use this when you must automatically pull all results in chunks, e.g., for:

ETL scripts

Memory-safe batch jobs

Long-running exports